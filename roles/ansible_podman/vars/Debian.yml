---
podman_packages:
  - "podman"
  - "podman-docker"
  - "conmon"
  - "aardvark-dns"
  - "netavark"
#   - "iptables" # Dunno what that bug was. I will disable it for now
# iptables only needed because of bug in Debian
