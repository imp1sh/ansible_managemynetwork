# Generated by Ansible - Dropbear Authorized Keys for OpenWrt
{% for key_id, key_details in system_sshkeys_deploy_on_hostgroups.items() %}
{% set host_matches = key_details.hostgroups | intersect(group_names) %}
{% if host_matches | length > 0 %}
{% set key_data = system_sshkeys[key_id] | default(none) %}
{% if key_data and key_data.state | default('present') == 'present' %}
# Key: {{ key_id }}
{{ key_data.type }} {{ key_data.key }} {{ key_id }}
{% endif %}
{% endif %}
{% endfor %}
