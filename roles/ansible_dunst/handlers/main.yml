#SPDX-License-Identifier: MIT-0
---
# Note: Dunst restart is now handled inline in the deploy tasks
# This handler is kept for backward compatibility but is no longer used
- name: restart dunst
  ansible.builtin.debug:
    msg: "Dunst restart is now handled inline in deploy tasks. This handler is deprecated."
  changed_when: false
